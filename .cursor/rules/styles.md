# Styling Guidelines

## Description

Use Tailwind CSS classes with className for styling. Use regular stylesheets only when specified.

## Rules

* Use Tailwind CSS classes with `className` prop for all styling.
* Use React Native StyleSheet when specifically requested.
* Follow the project's theme configuration in `global.css`.
